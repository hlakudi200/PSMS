## Summary
Build the teacher portal LayoutShell so every teacher route renders inside a consistent sidebar + topbar + ProtectedRoute, mirroring the principal portal pattern.

## Scope
- Add `frontend/src/app/teacher/layout.tsx` wrapping `LayoutShell`.
- Menu groups: Dashboard, My Classes, My Schedule, Materials, Lessons, Assessments, Attendance, Marks, Messages.
- `allowedRoles: ['Teacher']`, accent color from `roleColors.Teacher`.

## Acceptance Criteria
- [ ] Teacher navigates between sub-routes without re-mounting the shell.
- [ ] Non-Teacher roles are redirected.
- [ ] Sidebar collapse persists per the LayoutShell pattern.
- [ ] `npx tsc --noEmit` clean.

## Business Rules
N/A (foundation)

## Depends on
none -- blocks every other teacher ticket

---
Source: backend/docs/TEACHER-PORTAL-PLAN.md
