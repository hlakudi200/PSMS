## Summary
Assessments page + form modal + inline QuestionBuilder. (US-TCH-007)

## Scope
- `app/teacher/assessments/page.tsx` + `AssessmentFormModal` + `QuestionBuilder` panel.
- Assessment fields (Zod): classSubjectId, termId, type, maxMarks, weight, passPercentage, scheduledDate, dueDate, durationMinutes, instructions.
- QuestionBuilder: 5-100 questions, 2-6 options each, exactly one correct, text length bounds.

## Acceptance Criteria
- [ ] Cannot save with < 5 questions.
- [ ] Each question requires one correct option.
- [ ] Saving creates assessment + questions atomically.
- [ ] tsc clean.

## Business Rules
QA-001 (structure), QA-002 (scheduling).

## Depends on
#T-T01

---
Source: backend/docs/TEACHER-PORTAL-PLAN.md
